<section>
    <div class="container">
        <div class="box">
            <div class="content">
                {{#each posts as |post|}}
                <h4> {{title}} </h4>
                <article class="post">
                    {{post_body}}
                </article>
            </div>
        </div>
    </div>
</section>
{{/each}}
